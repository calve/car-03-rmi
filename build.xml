<project name="rmi" default="compile" basedir=".">
  <path id="classpath">
    <!-- <fileset dir="lib"> -->
    <!--   <include name="*.jar" /> -->
    <!-- </fileset> -->
  </path>

  <target name="compile">
    <mkdir dir="classes" />
    <javac srcdir="src" destdir="classes">
      <classpath refid="classpath"/>
    </javac>
  </target>
</project>
